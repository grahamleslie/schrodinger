<p id="notice"><%= notice %></p>

<h1>Pipelines</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Latest</th>
      <th>Runs</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @pipelines.each do |pipeline| %>
      <tr>
        <td><%= pipeline.name %></td>
        <td><%= pipeline.runs.count > 0 ? pipeline.runs[-1].status : 'Not yet run' %> <%= pipeline.runs.count > 0 ? "#{time_ago_in_words(pipeline.runs[0].created_at)} ago" : '' %></td>
        <td><%= pipeline.runs.count %></td>
        <td><%= link_to 'Show', pipeline %></td>
        <td><%= link_to 'Edit', edit_pipeline_path(pipeline) %></td>
        <td><%= link_to 'Destroy', pipeline, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Pipeline', new_pipeline_path %>
<%= link_to 'Manage Secrets', secrets_path %>
<%= link_to 'Scan Pipelines', check_pipelines_path %>

