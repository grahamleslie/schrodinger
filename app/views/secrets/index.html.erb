<p id="notice"><%= notice %></p>

<h1>Secrets</h1>
<p>When a Pipeline runs, any secrets with the same <code>domain</code> as the Pipeline will be injected as both build arguments and environment variables.</p>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Value</th>
      <th>Domain</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @secrets.each do |secret| %>
      <tr>
        <td><%= secret.name %></td>
        <td><%= secret.hidden_value %></td>
        <td><%= secret.domain %></td>
        <td><%= link_to 'Edit', edit_secret_path(secret) %></td>
        <td><%= link_to 'Destroy', secret, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Secret', new_secret_path %>
<%= link_to 'Back to Pipelines', pipelines_path %>

